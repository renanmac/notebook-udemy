<div class="form-wrapper">
	<h1>New Contact</h1>

	<%= render 'form', contact: @contact %>

	<%= link_to "<span class='glyphicon glyphicon-home' aria-hidden='true'> #{t('buttons.back')}</span>".html_safe, contacts_path, class: "btn btn-info" %>
</div>